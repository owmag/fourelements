# Four Elements

A fullscreen React app with four white quadrants split by a cross, and four words rotating on a circular SVG path centred on screen.

Built with React + Vite + TypeScript + GSAP.

## Run

```bash
npm install
npm run dev
```

Opens at http://localhost:5555
